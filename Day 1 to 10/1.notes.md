## Leetcode Notes for Solving problem while complex thinking

#### 1. *Charcter.isDigit(ch)*  is used to find digits in String.
#### 2. *map.computeIfAbsent(s, k -> new ArrayList<>()).add(str)*  is used to store freq of for the strigs like we store sthe freq in map.put(i,map.getOrdefault(i,0)+1) like that.
#### 3. *Integer.bitCount(num)*  this is used to count the bit in Integer.
#### 4. *map.put(nums[i], map.getOrDefault( nums[i], 0) +1)* this is find to numbers with their freq.
#### 4. *next greater* this is used to find the next greater element in given array here we start the loop from the end goes upto finding small elements at each time we are to put elemnt in the *stack*
#### -- logic is basically *while(!s.isEmpty() && a[s.peek()] <= a[i])* we pop the element from the stack.
